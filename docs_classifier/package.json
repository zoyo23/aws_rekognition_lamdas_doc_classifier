{
  "devDependencies": {
    "serverless": "^3.4.0",
    "serverless-offline": "^8.5.0"
  },
  "scripts": {
        "debug": "node --inspect node_modules/serverless/bin/serverless offline start --httpPort 4000 --noTimeout"
    }
}